[package]
name = "rbpf_elf"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^2.1.0", features = ["std", "heap-embedded-alloc", "unstable", "getrandom"] }
solana_rbpf = { path = "../../../", default-features = false }
test_utils = { path = "../../../test_utils" }